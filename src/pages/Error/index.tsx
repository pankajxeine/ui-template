import { Typography } from 'ud-ui-toolkit';
import { MUIdButton } from 'ud-ui-toolkit';

export const Error = (props: any) => {
    return (
        <>
            <Typography title=" Page Not Found" />
            <MUIdButton label="heelp" />
        </>
    )
}